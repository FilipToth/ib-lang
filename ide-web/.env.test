# Read by react-scripts when jest runs, so a test that depends on the config
# gets the same values on every machine instead of whatever .env.*.local holds.
REACT_APP_BACKEND_URL=http://test.invalid/api/
REACT_APP_WEBSOCKETS_URL=ws://test.invalid/wss
